<h1 class="text-center">vista perfil</h1>

<section>
    <h2>Datos del usuario.</h2>
    <img src="/public/uploads/{{usuario.imagen}}" alt="{{usuario.imagen}}">
    <p>Nombre: {{usuario.nombre}}</p>
    <p>Apellido: {{usuario.apellido}}</p>
</section>